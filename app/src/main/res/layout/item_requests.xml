<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <data>
    <variable
      name="request"
      type="el.ka.speechart.service.model.RequestToRegSpecialist" />
  </data>

  <androidx.cardview.widget.CardView
    android:id="@+id/wrapper"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

  <LinearLayout
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/app_padding"
    android:paddingVertical="10dp"
    android:layout_height="wrap_content">

    <TextView
      style="@style/text.header3"
      android:text="@{request.userData.fullName}"
      tools:text="Большаков Роман Станиславович" />

    <TextView
      android:layout_marginTop="5dp"
      style="@style/text.hint"
      android:text="@{request.userData.email}"
      tools:text="reuwappiheje-3869@yopmail.com" />
  </LinearLayout>

  </androidx.cardview.widget.CardView>

</layout>